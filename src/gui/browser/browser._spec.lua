local describe = _ENV.describe
local it = _ENV.it

describe('browser', function()
    it('Should default to the moonSHINE tab', function()
    end)

    it('Should display the available tabs', function()
    end)

    it('Should display the active tab content', function()
    end)

    it('Should change the main content on tab switch', function()
    end)

    it('Should forward interactions according to active tab', function()
    end)
end)
